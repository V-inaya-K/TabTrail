[project]
name = "tabtrail-backend"
version = "0.1.0"
description = "TabTrail Visual AI Browser Activity Tracker — Backend API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"